# Public assets

Coloca aquí los archivos estáticos servidos en la raíz del sitio:

- `cv.pdf` — currículum (enlazado desde el Footer)
- `og-image.png` — preview redes sociales, **1200×630 px**, <300KB
- `favicon.svg` — favicon (ya existe)
- `icons.svg` — sprite de íconos (ya existe)
- `robots.txt` — directivas para crawlers (ya existe)
- `sitemap.xml` — mapa del sitio (ya existe)

Los archivos en `public/` se sirven tal cual sin procesamiento Vite.
